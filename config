apiVersion: v1
clusters:
- cluster:
    certificate-authority-data: "$CERTIFICATE_AUTHORITY"
    server: "$SERVER"
  name: "$CLUSTER_NAME"
contexts:
- context:
    cluster: "$CLUSTER_NAME"
    user: "$USERNAME"
  name: "$CONTEXT_NAME"
current-context: "$CONTEXT_NAME"
kind: ""
users:
- name: "$USERNAME"
  user:
    token: "$TOKEN"
